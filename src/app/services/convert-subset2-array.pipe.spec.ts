import { ConvertSubset2ArrayPipe } from './convert-subset2-array.pipe';

describe('ConvertSubset2ArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSubset2ArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
