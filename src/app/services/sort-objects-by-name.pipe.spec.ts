import { SortObjectsByNamePipe } from './sort-objects-by-name.pipe';

describe('SortObjectsByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortObjectsByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
