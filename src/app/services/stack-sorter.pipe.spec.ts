import { StackSorterPipe } from './stack-sorter.pipe';

describe('StackSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new StackSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
